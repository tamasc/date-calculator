const assert = require('assert');
const DateCalculator = require('../date-calculator');

describe('DateCalculator', function() {
  it('is an object', function() {
    assert.ok(DateCalculator instanceof Object);
  });
});